# 企业级 Agent 落地实践：从"完成任务"到"业务循环持续运转"

做了一年多的 Agent 工程，我最大的体会是一个反直觉的事实：**让 Agent 回答一个难题很容易，让它持续扛起一条业务线很难。**

Chatbot 时代结束的时候，我们都以为"模型更强了，业务自动化就水到渠成"。但真把 Agent 放进业务里，你会发现它面对的不是"一个任务"，而是"永远在发生的流程"：SRE 不是修完一次故障就下班，而是 7×24 盯着告警、定位、应急、复盘、积累；数据运营不是出完一次月报就完事，而是按月取数、对账、签收、沉淀口径。真实业务没有"开始"和"结束"，只有周而复始的循环。

这篇文章不聊框架选型，不贴炫技代码，只讲三件事：我为什么认为业务场景的核心是"循环"而不是"任务"；我是怎么用三层循环把 Agent 嵌进业务流程的；以及如何让这套循环自己持续转下去、越转越快。

## 一、业务不是任务清单，是持续运转的循环

先把思考的坐标系搭起来。过去几年 AI 应用工程的演进，其实是一条"控制粒度不断上移"的路径：

| 时代 | 核心范式 | 解决了什么 | 留下了什么 |
|---|---|---|---|
| Prompt 时代 | 写更好的提示词 | 让 LLM 能"说话" | 无状态、无工具、对话结束一切蒸发 |
| Workflow 时代 | 人工设定流程节点 | 让 LLM 接工具、串流水线 | 流程刚性，现实和流程不符就崩 |
| ReAct 时代 | 全动态规划推进任务 | LLM 自己决定下一步 | 长程任务跑飞、上下文爆炸、死循环 |
| Harness 时代 | 精细化运行环境管理 | 控制上下文、约束执行 | 仍是一次性任务，经验不沉淀 |
| Loop 时代 | 设计循环 | AI 从"完成任务"走向"持续参与" | — |

前四个时代有个共同的隐含假设：**AI 处理的是一个有明确起止点的任务**。任务做完，对话结束，一切蒸发。但真实业务场景不是这样运行的——它是持续进行的事件流。这意味着，如果想让 Agent 原生地承担某个场景的工作，需要的不是"更强的单次任务能力"，而是**面向场景设计循环**：

- 持续监听接收任务（事件驱动，而不是人发起）
- 分析处理任务（自主规划，而不是人工编排）
- 完成交付并积累沉淀（产出归集，而不是用完即弃）
- 自主成长进化（越用越懂这个团队，而不是每次从零开始）

这就是行业里正在发生的范式转移：从 "Stop prompting" 到 "Design the loop"。我在自己的项目里把这套思路落成了三层嵌套的循环，从内到外是：L1 LLM Loop（模型层）、L2 Agent Loop（记忆层）、L3 业务场景 Loop（业务层）。三层不是三个功能，而是三种循环维度，逐层嵌套驱动。

## 二、L1 LLM Loop：让模型在预算内可控地跑完长程任务

最内层是 ReAct 式的循环：**思考 → 行动 → 验证**，循环往复直到任务完成。这层的关键难点不是"让它循环"，而是"让它循环 300 步不崩、不跑飞、不烧钱"。我在这层踩过太多坑，最终收敛出四道工程闸门：

**死循环检测。** LLM 卡住时会反复调用同一个工具、同一组参数。我们对"工具名 + 规范化参数"做哈希，连续 3 次相同调用就触发处置：放行、阻断、或者转人工确认。工具执行前检查，把"无限烧钱循环"扼杀在摇篮里。

**上下文压缩。** 长循环会产生海量历史消息。我们按预算把历史切成 hot / warm / cold 三层，冷层自动摘要压缩，发送前还有一道不变量检查与修复，保证送进模型的内容结构完整。

**工具输出截断。** 工具返回 10MB 日志会瞬间撑爆上下文。超限输出（默认 500 行 / 5KB）归档到文件系统，返回分页引用，Agent 需要时可以分页读回——既保护了上下文，又不丢信息。

**历史裁剪。** 基于上下文使用率触发修剪，保留一定的保护比例，让上下文始终有"呼吸空间"。

这层我的设计哲学很简单：**L1 的目标不是"让 LLM 一直跑"，而是"让 LLM 在预算步数内可控地跑完一个长程任务"。** 可控性来自工具面的硬闸门，而不是祈祷模型别出错。

## 三、L2 Agent Loop：让跑过的任务变成下次的加速

L1 解决了"单次任务跑得稳"，但有个致命问题：每次任务开始，Agent 还是白纸一张——上个月踩过的坑，这个月照样踩。L2 层要解决的是**让经验沉淀下来，让 Agent 越用越准**。

我们借鉴了人类记忆的机制，做了三件事：

**记忆三级进化。** 按频率分级沉淀：每轮轻量写入原始记忆（verbats）；每 10 轮做一次跨轮反思，去重、精炼，整理成文档级记忆并建立溯源关系；会话结束时做彻底整理——高频召回的晋升为长期记忆，陈旧的自然归档。这是"在线学习"式的记忆进化：边跑边沉淀，不是等结束才总结。

**召回追踪。** 记忆只有"被用上"才有价值。我们持久化记录每条记忆被召回的次数、累计相关度、被哪些查询召回、跨越多少天仍被召回。这些数据是记忆晋升的评判依据——不靠"感觉"决定哪条记忆重要，用真实召回数据说话。

**记忆晋升引擎。** 借鉴睡眠三阶段：浅睡（从召回历史收集候选）、快速眼动（模式识别与概念标签分析）、深睡（多维评分并晋升写入）。评分有六个维度，权重最高的是相关度和召回频次，另外还有多样性（被多少不同问题召回）、近因性、持续有效性、概念标签数。晋升有硬门槛（阈值 0.5），保证只有真正反复被需要、跨多个问题、跨多天仍然有用的记忆，才被固化成长期记忆。

这层的设计哲学是：**L2 的目标不是"记住所有事"，而是"让真正有用的记忆浮上来，让无用的沉下去"。** Agent 不再是每次从零开始，而是带着过去 N 次任务的经验迎接新任务。

## 四、L3 业务场景 Loop：把 Agent 变成业务循环里的角色

前两层仍在"任务"维度循环——任务结束，循环结束。但真实场景是持续进行的。L3 层把循环扩展到业务场景层，让 Agent 在场景里变成一个真实角色，自主持续参与。

这层我用四个词来概括落地骨架：**空间、触发、剧本、交付**。

**空间（Workspace）：一个有准备的工作环境。** 数据已接好、口径已定义、方法已固化、权限已配好，Agent 进来就能干活，人只在关键节点介入。空间不是"一个对话"，是"一个环境"——这是产品单元从"对话"到"环境"的升级。我们给空间定的北极星指标叫**沉淀厚度**：一个新成员（人或 Agent）进入空间，多快能达到"老师傅"的工作水平。

**触发（Trigger）：持续监听，而不是等人发起。** 一个业务场景的循环入口统一支持四种触发源：定时（比如每周一早上跑巡检）、Webhook（PR 合并、CI 事件）、告警（监控告警接入）、手动（对话里说一句"跑一次巡检"）。四种触发统一走一条链路创建任务，异步启动执行，不阻塞调用方。这是 L3 的"事件驱动"特性：Agent 不是等人发指令，而是持续监听场景事件。

**剧本（Playbook）：策略声明，而不是工作流脚本。** 这是我在实践中最重要的一个范式选择。剧本不规定"怎么做"，只规定"必须满足什么约束、能用什么资源、必须产出什么"：

```yaml
playbook:
  skills:        # 这个场景能用哪些 Skill（Agent 自己选怎么用）
  context:       # 执行前强制加载的资产和资源
  gates:         # 不变量——某些条件下必须人介入
  deliverables:  # 必须产出什么、怎么分发
  distill:       # 必须沉淀什么
```

做法让 Agent 自己决定——LLM 越强，执行越好。如果写成 workflow 步骤脚本，LLM 编排能力的升级就无从受益。这是"声明式优于过程式"的典型体现。

**交付：产出不只是"最后一条消息"。** 我们定义了四类交付，覆盖交付物的所有去向：Notify（把产出推给人或群，比如邮件发报告）、Publish（把产出写入外部持久系统，比如报表入 BI）、Execute（把操作计划在真实世界执行，比如重启服务、部署代码）、Host（把产出在空间内托管、展示、运行）。其中 Host 类是核心差异化：空间不只是工作发生的地方，还是交付物栖居的地方——SRE 空间长期托管运维操作历史，运营空间托管看板和 notebook。

到此为止，一次"触发 → 执行 → 交付"的闭环就通了。但要让循环"持续转起来"，还差最关键的一环：**沉淀**。

## 五、让循环转起来：五环数据飞轮

一个循环如果只是"跑完就结束"，那它永远只是自动化，不是飞轮。飞轮和自动化的区别在于：**每一次循环都必须留下点什么，让下一次循环更快、更准。**

我们把每次执行的产物接进一条五环数据飞轮：

```
资产(Asset) → Agent → Trace → 演化(Evolution) → 评测(Evaluation) → 回到资产
```

五环通过共享事件总线联动，具体是：

**资产环。** 每次任务结束强制沉淀——Task 关闭前校验沉淀完整性，未完成不允许关闭。L2 记忆单向沉淀为组织资产，资产成熟度经历 draft → proposed → confirmed → published → canonical 五级晋升，人确认后可供同空间其他 Agent 复用。个体经验变成组织资产，这是"越用越厚"的起点。

**Agent 环。** Agent 在场景里成长，按执行量、成功率、资产贡献、背书等维度多维评分，驱动 novice → proficient → expert → master 四阶段跃迁，阶段决定权限——这是"渐进信任"：Agent 用实绩换取更大的自主权。

**Trace 环。** 每次执行采集完整轨迹，结束时发布事件。轨迹是飞轮的"数据燃料"。

**演化环。** 定期扫描某个剧本最近 N 次执行，统计 Skill 实际调用模式，识别"反复出现的额外步骤"或"反复被跳过的步骤"，生成剧本修改提议推送给负责人审批。注意：**AI 不能自动改剧本**——所有提议必须人审批后才生效。

**评测环。** 对 Agent 产出做评测，评分回写成熟度，反哺 Agent 环。

五环咬合：Agent 执行 → 采集 Trace → 沉淀为 Asset → 评测反馈 → 演化提效 → 反哺 Agent 成长。**每一环都把产出喂给下一环**，业务数据越跑，飞轮转得越快。这就是"业务循环持续运行"的工程实现——不是靠人天天盯着，而是靠数据自己驱动。

## 六、人机分工：人在阀门上，不在流水线里

说到这，必须回答一个绕不开的问题：全自动了，人干嘛？

我的答案是：**Agent 自动跑，跑到需要人的地方，变成一条待办，出现在该出现的人的收件箱里。人不在流水线里，人在流水线的阀门上。**

所有人工阀门汇于一个收件箱：执行卡住时的介入、语义确认、交付审批、转交、手动待办。人处理完阀门，流水线继续自动跑。配合"渐进自主"的成熟度机制，Agent 在低风险环节先自动跑（只报告），跑出信用后逐步放开到无人值守——信任是挣来的，不是给的。

## 七、三条用代价换来的工程纪律

实践一年多，很多想当然的设计都被现实教育过。最后沉淀出三条纪律，我认为比任何架构都重要：

**纪律一：治理落工具面硬门禁，prompt 只做引导。** prompt 软约束打不过工具可用性。权限门禁、Execute 类审批、语义口径，都不靠 prompt 告诉 Agent"请不要做"，而是从工具面直接限制 Agent 能调什么、能碰什么。这是实测换来的教训：你永远不知道模型会怎么理解你的"请求"。

**纪律二：声明式优于过程式。** 剧本用声明式 DSL（skills + context + gates + deliverables + distill），而不是 workflow 步骤脚本。这背后的逻辑是：**把编排权留给 LLM**，声明式、不限制，LLM 越强执行越好；过程式流程把模型的能力上限锁死在了写 DSL 的那一天。

**纪律三：AI 提议，人决策。** 剧本演化、语义确认、Execute 类执行——所有涉及"改变"的决策，AI 只识别 + 提议，永远人审批。AI 改自己的剧本是红线。这是让 AI"自主但不失控"的关键边界。

## 八、再进一步：从 Loop 到 Graph

文章最后说一个我正在探索的方向。Loop 解决的是"一个角色把局部任务做完"，但真实业务往往需要"多个局部任务组成可交付的结果"——采集、校验、分析、审批、发布，各有各的角色和关口。

行业里这波讨论叫 Graph Engineering：把藏在 Prompt 和上下文里的职责、状态、路由、检查、退路，变成显式、可执行、可追踪的图结构。判断该不该用图，社区给过五条信号：路径会随中间结果变化、有多个可独立完成的专业任务、需要长时间运行、需要多道质量检查、涉及高风险动作。对照下来，SRE 巡检、数据月报这类场景全中——它们是典型该上"图"的业务。

但有一条底线我坚持：**Graph 是 Loop 的组织升级，不是替代。** 单 Agent 长程任务用 Loop 最直接，不需要也不应该改；只有多角色、动态路由、需中途校验、高风险的场景才值得显式编排。不要为了框架而框架——单次调用能解决的事，别上 Workflow；Workflow 能解决的，别上 Loop；Loop 能解决的，别上 Graph。

## 写在最后

回到开头的判断：模型能力在趋同，真正拉开差距的，是**如何让模型在真实场景里持续、可控、进化地工作**。

个人 Agent 的循环止于"任务完成"，团队 Agent 的循环止于"场景沉淀"。当你把 Agent 从"被调用时才存在"变成"始终在场、持续成长"，它就不再是工具，而是业务循环里的一个真实角色。让业务循环持续转起来，靠的不是更强的模型，而是更好的循环设计——以及每一次循环都留下复利。

这是我的实践，欢迎讨论。
